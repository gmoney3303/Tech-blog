<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog Post</title>
  <!-- Include any necessary stylesheets or scripts -->
</head>

<body>
  <header>
    <h1>Tech Blog Post</h1>
    <!-- Navigation or back button to go to the blog post list or another page -->
  </header>

  <main>
    <div class="blog-post">
      <h2>{{post.title}}</h2>
      <p>Author: {{post.author}}</p>
      <p>Date Posted: {{format_date post.date}}</p>
      <img src="{{post.image}}" alt="Post Image">
      <p>{{{post.content}}}</p>
      <!-- Additional content/details of the blog post -->
    </div>

    <!-- Add comment section if applicable -->
    {{!-- Example comment section --}}
    <div class="comments-section">
      <h3>Comments</h3>
      <ul>
        {{#each post.comments}}
        <li>
          <p>{{commentText}}</p>
          <p>By: {{commenter}}</p>
          <p>Date: {{format_date createdAt}}</p>
        </li>
        {{/each}}
      </ul>
      <!-- Form to add a new comment -->
      <form class="comment-form">
        <textarea name="comment" placeholder="Add your comment"></textarea>
        <button type="submit">Submit</button>
      </form>
    </div>
  </main>

  <footer>
    <!-- Footer content if needed -->
  </footer>

  <script src="./js/project.js"></script>
</body>

</html>
